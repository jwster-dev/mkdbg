Phase 1a note

This directory contains MCU startup assembly used by the firmware target.

Phase 1a leaves startup code unchanged. Current work is focused on host build
structure and mkdbg-native modularization, not boot-path behavior.
